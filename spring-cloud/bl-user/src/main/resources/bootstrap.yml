spring:
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: bl-config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/